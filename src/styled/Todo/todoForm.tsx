import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
    input: {
        width: "55%",
        margin: "0 auto"
    },

    button: {
        marginTop: "20px",
        marginBottom: "10px"
    }

}));
